### commercetools Environment Variables

## API & Auth URLs
# CT_API_URL is usually 'https://api.us-central1.gcp.commercetools.com' and CT_AUTH_URL 'https://auth.us-central1.gcp.commercetools.com'. 
# These may change depending on the commercetools location you chose for your project.
export CT_API_URL="{commercetoolsApiUrl}"
export CT_AUTH_URL="commercetoolsAuthUrl"

## Project Name
export CT_PROJECT_ID="{commercetoolsProjectId}"

## API Client
export CT_CLIENT_ID="{commercetoolsClientId}"
export CT_CLIENT_SECRET="{commercetoolsClientSecret}"
# TODO: use a narrower scope list (only what is needed for the commercetools setup and the connector functionality)
export CT_SCOPES="manage_project:${CT_PROJECT_ID}"

## API Extension
export CT_API_EXTENSION_NAME="planetpayment-connector-extension"

# If the variable CT_API_EXTENSION_URL is provided, the deploy script will create a commercetools API extension of destination type "HTTP".
# This is the URL that the deployed connector can be accessed via HTTP. This will be used by commercetools.
# If the variable CT_API_EXTENSION_URL is not provided, type "AWSLambda" will be used instead. Make sure to define these if you prefer to use type "AWSLambda".
export CT_API_EXTENSION_URL="{commercetoolsApiExtensionUrl}"

## AWS Environment Variables
# Variables necessary to setup the commercetools API extension of destination type "AWSLambda". 
# You will have to define an Amazon Resource Name (ARN), and an access key & secret to access the connector deployed as an AWS Lambda. 
export CT_API_EXTENSION_AWS_LAMBDA_ARN="{AwsLambdaArn}"
export CT_API_EXTENSION_AWS_LAMBDA_ACCESS_KEY="{AwsLambdaAccessKey}"
export CT_API_EXTENSION_AWS_LAMBDA_SECRET="{AwsLambdaSecret}"

<<<<<<< HEAD:deploy/env.example

##### Variables related to Datatrans

=======
### Datatrans Environment Variables
export DT_MERCHANTS='[{"id": "{merchantId}", "password": "{merchantIdPassword}", "environment": "{environment}", "dtHmacKey": "{sign}"}]'
>>>>>>> a5feb9d (Added AWS deploy docs, corrected links, shortened .env and removed unnecessary readme.md files):deploy/.env
export DT_CONNECTOR_WEBHOOK_URL="URL where the deployed Connector is accessible via HTTP (to be used by Datatrans)"
